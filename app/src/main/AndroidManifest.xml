<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cscodetech.dailymilk">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyMilk.NoActionBar"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.SpinAndwinActivity"
            android:exported="true" />
        <activity android:name=".ui.NotificationActivity" />
        <activity android:name=".ui.PrivacyActivity" />
        <activity android:name=".ui.AboutActivity" />
        <activity android:name=".ui.SupportActivity" />
        <activity android:name=".ui.ReferlActivity" />
        <activity android:name=".ui.OrderDetailsActivity" />
        <activity android:name=".ui.SubOrderDetailsActivity" />
        <activity android:name=".ui.MySubcriptionListActivity" />
        <activity android:name=".ui.MyOrderListActivity" />
        <activity android:name=".ui.SearchActivity" />
        <activity android:name=".ui.CollectionProductActivity" />
        <activity android:name=".ui.WalletActivity" />
        <activity android:name=".ui.AddressActivity" />
        <activity android:name=".ui.CartActivity" />
        <activity android:name=".ui.CreateSubscriptionActivity" />
        <activity android:name=".ui.ProductListActivity" />
        <activity android:name=".ui.DiscountActivity" />
        <activity android:name=".ui.CollectonActivity" />
        <activity android:name=".ui.FreshStockActivity" />
        <activity android:name=".ui.CategoryActivity" />
        <activity android:name=".locationpick.LocationGetActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.VerifyPhoneActivity" />
        <activity android:name=".ui.CityActivity" />
        <activity android:name=".ui.CompleOrderActivity" />
        <activity android:name=".ui.CoupunActivity" />
        <activity android:name=".ui.ForgotActivity" />
        <activity android:name=".ui.ChanegPasswordActivity" />
        <activity android:name=".ui.RazerpayActivity" />
        <activity android:name=".ui.PaypalActivity" />
        <activity android:name=".ui.StripPaymentActivity" />
        <activity android:name=".ui.WallateHistryActivity" />
        <activity
            android:name=".ui.LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.IntroActivity" />
        <activity android:name=".ui.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>